import { ComingSoon } from '@/components/shared/ComingSoon';

export default function NotesPage() {
  return (
    <ComingSoon
      feature="Notes"
      description="学習中に取ったメモを一元管理。チャプターやタグでフィルタリングし、復習時に活用できます。"
    />
  );
}
