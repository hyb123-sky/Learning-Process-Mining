# PathForge dev environment.
# This file is committed deliberately — for local SQLite the URL has no secrets.
# For production / Turso / Postgres values, use .env.local (gitignored).

DATABASE_URL="file:./dev.db"
